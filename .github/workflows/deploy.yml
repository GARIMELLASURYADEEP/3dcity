name: Disabled - Pages deploy (repository converted to static site)

# The repository was converted to a static-only site (HTML/CSS/JS).
# This workflow has been disabled to avoid running Node builds.
# If you want to re-enable GitHub Pages deployment, replace this file
# with a workflow that only publishes the existing `dist/` folder or
# publishes the repository root as a static site.

